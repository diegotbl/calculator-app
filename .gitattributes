# Go tooling (gofmt, go vet) expects LF line endings. Without this, Windows
# checks these files out as CRLF and `gofmt -l` reports every file as misformatted.
*.go text eol=lf

# Keep SVG assets LF so they don't churn between Windows and CI checkouts.
*.svg text eol=lf
